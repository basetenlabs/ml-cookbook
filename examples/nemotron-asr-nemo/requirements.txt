# The NeMo framework container already provides nemo_toolkit[asr] and its heavy
# audio deps (librosa, soundfile, kaldialign, huggingface-hub, etc.). Keep this
# minimal and unpinned so we don't downgrade the container's tested stack -
# hf_transfer is the only extra the checkpoint download benefits from.
hf_transfer
